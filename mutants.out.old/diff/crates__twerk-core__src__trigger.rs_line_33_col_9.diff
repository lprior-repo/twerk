--- crates/twerk-core/src/trigger.rs
+++ replace <impl fmt::Display for TriggerState>::fmt -> fmt::Result with Ok(Default::default())
@@ -25,23 +25,17 @@
         write!(f, "unknown TriggerState: {}", self.0)
     }
 }
 
 impl std::error::Error for ParseTriggerStateError {}
 
 impl fmt::Display for TriggerState {
     fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
-        let name = match self {
-            Self::Active => "ACTIVE",
-            Self::Paused => "PAUSED",
-            Self::Disabled => "DISABLED",
-            Self::Error => "ERROR",
-        };
-        f.write_str(name)
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl FromStr for TriggerState {
     type Err = ParseTriggerStateError;
 
     fn from_str(s: &str) -> Result<Self, Self::Err> {
         match s.to_uppercase().as_str() {
