--- crates/twerk-core/src/repository_inmemory.rs
+++ replace == with != in <impl Repository for InMemoryRepository>::get_active_scheduled_jobs
@@ -311,17 +311,17 @@
         sjs.insert(id_str, sj.clone());
         Ok(())
     }
 
     async fn get_active_scheduled_jobs(&self) -> Result<Vec<ScheduledJob>> {
         let sjs = self.scheduled_jobs.read();
         Ok(sjs
             .values()
-            .filter(|sj| sj.state == crate::job::ScheduledJobState::Active)
+            .filter(|sj| sj.state != /* ~ changed by cargo-mutants ~ */ crate::job::ScheduledJobState::Active)
             .cloned()
             .collect())
     }
 
     async fn get_scheduled_jobs(
         &self,
         _current_user: &str,
         page: i64,
