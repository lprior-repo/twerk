[package]
name = "twerk-web-proofs"
version = "0.0.1"
edition = "2021"

[workspace]

[dependencies]
twerk-web = { path = "..", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = { version = "1.0" }

[profile.release]
debug = true
