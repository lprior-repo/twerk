--- crates/twerk-core/src/id.rs
+++ replace <impl Deref for TriggerId>::deref -> &Self::Target with Box::leak(Box::new(Default::default()))
@@ -202,17 +202,17 @@
         &self.0
     }
 }
 
 impl Deref for TriggerId {
     type Target = str;
 
     fn deref(&self) -> &Self::Target {
-        &self.0
+        Box::leak(Box::new(Default::default())) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl Borrow<str> for TriggerId {
     fn borrow(&self) -> &str {
         &self.0
     }
 }
