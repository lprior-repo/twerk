--- crates/twerk-core/src/repository_inmemory.rs
+++ replace <impl Repository for InMemoryRepository>::get_job_log_parts -> Result<Page<TaskLogPart>> with Ok(Page::new())
@@ -270,19 +270,17 @@
 
     async fn get_job_log_parts(
         &self,
         job_id: &str,
         _q: &str,
         page: i64,
         size: i64,
     ) -> Result<Page<TaskLogPart>> {
-        let logs = self.job_logs.read();
-        let parts = logs.get(job_id).cloned().unwrap_or_default();
-        Ok(Self::paginate_vec(parts, page, size))
+        Ok(Page::new()) /* ~ changed by cargo-mutants ~ */
     }
 
     async fn get_jobs(
         &self,
         _current_user: &str,
         _q: &str,
         page: i64,
         size: i64,
