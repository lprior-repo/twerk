--- crates/twerk-core/src/repository_inmemory.rs
+++ replace == with != in <impl Repository for InMemoryRepository>::get_all_tasks_for_job
@@ -121,17 +121,17 @@
     async fn get_all_tasks_for_job(&self, job_id: &str) -> Result<Vec<Task>> {
         let tasks = self.tasks.read();
         let job_id_str = job_id.to_string();
         Ok(tasks
             .values()
             .filter(|t| {
                 t.job_id
                     .as_ref()
-                    .map(|j| j.as_str() == job_id_str)
+                    .map(|j| j.as_str() != /* ~ changed by cargo-mutants ~ */ job_id_str)
                     .unwrap_or(false)
             })
             .cloned()
             .collect())
     }
 
     async fn get_next_task(&self, parent_task_id: &str) -> Result<Task> {
         let tasks = self.tasks.read();
