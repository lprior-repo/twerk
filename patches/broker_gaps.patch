# Broker Parity Gap Patch - Rust vs Go
# File: /home/lewis/src/twerk/tork/broker/mod.rs
#
# This patch documents all gaps between Go broker (tork-go) and Rust broker (twerk)
# The Rust code should NOT be modified - this is an audit document.

---

## GAP 1: Missing Broker Type Constants

### Go Code (/tmp/tork-go/broker/broker.go):
```go
const (
	BROKER_INMEMORY     = "inmemory"
	BROKER_RABBITMQ     = "rabbitmq"
	TOPIC_JOB           = "job.*"
	TOPIC_JOB_COMPLETED = "job.completed"
	TOPIC_JOB_FAILED    = "job.failed"
	TOPIC_SCHEDULED_JOB = "scheduled.job"
)
```

### Rust Code (/home/lewis/src/twerk/tork/broker/mod.rs):
```rust
/// Topic constant for job progress events
pub const TOPIC_JOB_PROGRESS: &str = "job.progress";
```

### Expected Fix:
Add to /home/lewis/src/twerk/tork/broker/mod.rs:
```rust
/// Broker type constants (Go: BROKER_*)
pub const BROKER_INMEMORY: &str = "inmemory";
pub const BROKER_RABBITMQ: &str = "rabbitmq";

/// Topic constants (Go: TOPIC_*)
pub const TOPIC_JOB: &str = "job.*";
pub const TOPIC_JOB_COMPLETED: &str = "job.completed";
pub const TOPIC_JOB_FAILED: &str = "job.failed";
pub const TOPIC_SCHEDULED_JOB: &str = "scheduled.job";
```

---

## GAP 2: Missing Provider Type Alias

### Go Code (/tmp/tork-go/broker/broker.go):
```go
type Provider func() (Broker, error)
```

### Rust Code:
No equivalent Provider type exists in Rust broker module.

### Expected Fix:
Add to /home/lewis/src/twerk/tork/broker/mod.rs:
```rust
/// Broker provider function type (Go: type Provider func() (Broker, error))
pub type Provider = Arc<dyn Fn() -> BoxedFuture<Box<dyn Broker + Send + Sync>> + Send + Sync>;
```

---

## GAP 3: Missing Context Parameters in Broker Trait Methods

### Go Code (/tmp/tork-go/broker/broker.go):
```go
type Broker interface {
	PublishTask(ctx context.Context, qname string, t *tork.Task) error
	SubscribeForTasks(qname string, handler func(t *tork.Task) error) error

	PublishTaskProgress(ctx context.Context, t *tork.Task) error
	SubscribeForTaskProgress(handler func(t *tork.Task) error) error

	PublishHeartbeat(ctx context.Context, n *tork.Node) error
	SubscribeForHeartbeats(handler func(n *tork.Node) error) error

	PublishJob(ctx context.Context, j *tork.Job) error
	SubscribeForJobs(handler func(j *tork.Job) error) error

	PublishEvent(ctx context.Context, topic string, event any) error
	SubscribeForEvents(ctx context.Context, pattern string, handler func(event any)) error

	PublishTaskLogPart(ctx context.Context, p *tork.TaskLogPart) error
	SubscribeForTaskLogPart(handler func(p *tork.TaskLogPart)) error

	Queues(ctx context.Context) ([]QueueInfo, error)
	QueueInfo(ctx context.Context, qname string) (QueueInfo, error)
	DeleteQueue(ctx context.Context, qname string) error
	HealthCheck(ctx context.Context) error
	Shutdown(ctx context.Context) error
}
```

### Rust Code (/home/lewis/src/twerk/tork/broker/mod.rs):
```rust
pub trait Broker: Send + Sync {
    fn publish_task(&self, qname: String, task: &Task) -> BoxedFuture<()>;
    fn subscribe_for_tasks(&self, qname: String, handler: TaskHandler) -> BoxedFuture<()>;

    fn publish_task_progress(&self, task: &Task) -> BoxedFuture<()>;
    fn subscribe_for_task_progress(&self, handler: TaskProgressHandler) -> BoxedFuture<()>;

    fn publish_heartbeat(&self, node: Node) -> BoxedFuture<()>;
    fn subscribe_for_heartbeats(&self, handler: HeartbeatHandler) -> BoxedFuture<()>;

    fn publish_job(&self, job: &crate::job::Job) -> BoxedFuture<()>;
    fn subscribe_for_jobs(&self, handler: JobHandler) -> BoxedFuture<()>;

    fn publish_event(&self, topic: String, event: serde_json::Value) -> BoxedFuture<()>;
    fn subscribe_for_events(&self, pattern: String, handler: EventHandler) -> BoxedFuture<()>;

    fn publish_task_log_part(&self, part: &crate::task::TaskLogPart) -> BoxedFuture<()>;
    fn subscribe_for_task_log_part(&self, handler: TaskLogPartHandler) -> BoxedFuture<()>;

    fn queues(&self) -> BoxedFuture<Vec<QueueInfo>>;
    fn queue_info(&self, qname: String) -> BoxedFuture<QueueInfo>;
    fn delete_queue(&self, qname: String) -> BoxedFuture<()>;

    fn health_check(&self) -> BoxedFuture<()>;
    fn shutdown(&self) -> BoxedFuture<()>;
}
```

### Expected Fix:
Update Broker trait in /home/lewis/src/twerk/tork/broker/mod.rs to accept context:
```rust
pub trait Broker: Send + Sync {
    fn publish_task(&self, ctx: Arc<()>, qname: String, task: &Task) -> BoxedFuture<()>;
    fn subscribe_for_tasks(&self, qname: String, handler: TaskHandler) -> BoxedFuture<()>;
    // ... etc
}
```

NOTE: This is a BREAKING CHANGE to the Rust API. The context parameter allows for cancellation, timeouts, and tracing propagation matching Go semantics.

---

## GAP 4: QueueInfo Field Type Mismatch (int vs i64)

### Go Code (/tmp/tork-go/broker/queues.go):
```go
type QueueInfo struct {
	Name        string `json:"name"`
	Size        int    `json:"size"`
	Subscribers int    `json:"subscribers"`
	Unacked     int    `json:"unacked"`
}
```

### Rust Code (/home/lewis/src/twerk/tork/broker/mod.rs):
```rust
pub struct QueueInfo {
    pub name: String,
    pub size: i64,
    pub subscribers: i64,
    pub unacked: i64,
}
```

### Expected Fix:
Change Rust QueueInfo to use i32 to match Go's int (or document the type mismatch):
```rust
pub struct QueueInfo {
    pub name: String,
    pub size: i32,       // Changed from i64 to match Go's int
    pub subscribers: i32,
    pub unacked: i32,
}
```

---

## GAP 5: Inconsistent TOPIC_SCHEDULED_JOB Value

### Go Code (/tmp/tork-go/broker/broker.go):
```go
TOPIC_SCHEDULED_JOB = "scheduled.job"  // dot, not hyphen
```

### Rust Code (Found in multiple locations):
- `/home/lewis/src/twerk/coordinator/coordinator.rs:70`: `const TOPIC_SCHEDULED_JOB: &str = "scheduled-job";`  // HYPHEN
- `/home/lewis/src/twerk/coordinator/config.rs:67`: `const TOPIC_SCHEDULED_JOB: &str = "scheduled-job";`  // HYPHEN
- `/home/lewis/src/twerk/coordinator/api/handlers.rs:27`: `const TOPIC_SCHEDULED_JOB: &str = "scheduled.job";`  // DOT

### Expected Fix:
Standardize on Go's value `"scheduled.job"` (with dot) in all Rust locations:
- Change `coordinator/coordinator.rs` line 70: `"scheduled-job"` -> `"scheduled.job"`
- Change `coordinator/config.rs` line 67: `"scheduled-job"` -> `"scheduled.job"`
- The `coordinator/api/handlers.rs` line 27 is already correct

---

## GAP 6: Topic Constants Should Be in tork/broker Module

### Issue:
Topic constants are scattered across the codebase:
- `engine/lib.rs`: TOPIC_JOB, TOPIC_JOB_COMPLETED, TOPIC_JOB_FAILED, TOPIC_JOB_PROGRESS
- `coordinator/handlers/job/mod.rs`: TOPIC_JOB_COMPLETED, TOPIC_JOB_FAILED
- `coordinator/coordinator.rs`: TOPIC_SCHEDULED_JOB
- `coordinator/api/handlers.rs`: TOPIC_SCHEDULED_JOB
- `coordinator/config.rs`: TOPIC_SCHEDULED_JOB
- `tork/broker/mod.rs`: only TOPIC_JOB_PROGRESS

### Expected Fix:
Move all topic constants to `tork/broker/mod.rs` and re-export from other modules:
```rust
// In tork/broker/mod.rs
pub const TOPIC_JOB: &str = "job.*";
pub const TOPIC_JOB_COMPLETED: &str = "job.completed";
pub const TOPIC_JOB_FAILED: &str = "job.failed";
pub const TOPIC_JOB_PROGRESS: &str = "job.progress";
pub const TOPIC_SCHEDULED_JOB: &str = "scheduled.job";
```

Then import from broker module in other files instead of defining locally.

---

## Summary of Required Changes to /home/lewis/src/twerk/tork/broker/mod.rs:

1. Add BROKER_INMEMORY and BROKER_RABBITMQ constants
2. Add TOPIC_JOB, TOPIC_JOB_COMPLETED, TOPIC_JOB_FAILED, TOPIC_SCHEDULED_JOB constants
3. Add Provider type alias
4. Update QueueInfo to use i32 instead of i64 (or document intentional difference)
5. Consider adding context parameter to broker methods (breaking change)
