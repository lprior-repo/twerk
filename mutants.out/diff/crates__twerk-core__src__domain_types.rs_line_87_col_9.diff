--- crates/twerk-core/src/domain_types.rs
+++ replace <impl Deref for QueueName>::deref -> &Self::Target with Box::leak(Box::new(Default::default()))
@@ -79,17 +79,17 @@
         &self.0
     }
 }
 
 impl Deref for QueueName {
     type Target = str;
 
     fn deref(&self) -> &Self::Target {
-        &self.0
+        Box::leak(Box::new(Default::default())) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 // ---------------------------------------------------------------------------
 // CronExpression
 // ---------------------------------------------------------------------------
 
 /// A validated cron expression.
