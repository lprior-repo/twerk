--- crates/twerk-core/src/id.rs
+++ replace <impl Deserialize<'de> for TriggerId>::deserialize -> Result<Self, D::Error> with Ok(Default::default())
@@ -127,18 +127,17 @@
 #[serde(transparent)]
 pub struct TriggerId(String);
 
 impl<'de> Deserialize<'de> for TriggerId {
     fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
     where
         D: serde::Deserializer<'de>,
     {
-        let s: String = String::deserialize(deserializer)?;
-        Self::new(s).map_err(serde::de::Error::custom)
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl TriggerId {
     /// Minimum length for a valid `TriggerId`.
     const MIN_LENGTH: usize = 3;
 
     /// Maximum length for a valid `TriggerId`.
